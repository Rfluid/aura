[package]
name              = "aura"
version           = "0.1.38"
edition.workspace = true
license.workspace = true

# Master crate: the `aura` binary. Headless subcommands go to `aura-cli`;
# no subcommand launches the tray app from `aura-ui`.
[dependencies]
anyhow.workspace   = true
aura-cli.workspace = true
aura-ui.workspace  = true
